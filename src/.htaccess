# Apache config — only read on Apache-based hosting (e.g. cPanel/shared hosting).
# Ignored by GitHub Pages/Netlify/Vercel; harmless to leave in place otherwise.

RewriteEngine On

# Force https
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Force a single canonical host: apex -> www
RewriteCond %{HTTP_HOST} ^vidhicorplegal\.com [NC]
RewriteRule ^(.*)$ https://www.vidhicorplegal.com/$1 [L,R=301]

# Redirect the client's former practice domain here, preserving deep links.
# pramahlawmen.com is NOT owned by the client and is deliberately absent.
RewriteCond %{HTTP_HOST} ^(www\.)?atblegalservices\.com [NC]
RewriteRule ^(.*)$ https://www.vidhicorplegal.com/$1 [L,R=301]

ErrorDocument 404 /404.html

# Basic caching for static assets
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType video/mp4 "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>
